import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appmovil.angular',
  appName: 'santiago-angular',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
